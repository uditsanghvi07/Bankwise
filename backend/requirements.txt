fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
langgraph==0.2.63
langchain-core==0.3.51
langchain-openai==0.2.14
pytest==8.3.4
pytest-asyncio==0.25.0
fpdf2==2.8.4
numpy>=1.26,<3.0

# --- Advanced RAG (optional, lazy-loaded). The app falls back to BM25-only
# retrieval if any of these fail to import — so installs without GPUs / large
# disks still work. Uncomment / install in the deployment environment that
# can host them (Docker image, RAG-enabled setup, etc.).
# sentence-transformers==3.3.1
# chromadb==0.5.23
