import { BrandLoader } from "@/components/layout/BrandLoader";

export default function RootLoading() {
  return (
    <BrandLoader
      title="Spinning up BankWise…"
      subtitle="Loading your GenAI banking console — calculators, RAG, and the agent loop."
    />
  );
}
